# =============================================================================
# EMBEDDING CONFIGURATION
# =============================================================================
# Provider type: "azure" for cloud embeddings, "local" for HuggingFace models
EMBEDDING_PROVIDER=azure

# --- Azure OpenAI Embeddings (when EMBEDDING_PROVIDER=azure) ---
EMBEDDING_API_KEY=your_azure_api_key_here
EMBEDDING_API_BASE=https://your-resource.openai.azure.com/openai/v1/
EMBEDDING_MODEL_NAME=text-embedding-3-large

# --- Local HuggingFace Embeddings (when EMBEDDING_PROVIDER=local) ---
# EMBEDDING_PROVIDER=local
# EMBEDDING_MODEL_NAME=BAAI/bge-large-en-v1.5
# EMBEDDING_DEVICE=cpu
# EMBEDDING_MAX_LENGTH=512

# =============================================================================
# LLM CONFIGURATION (Azure OpenAI only)
# =============================================================================
LLM_API_KEY=your_azure_api_key_here
LLM_API_BASE=https://your-resource.openai.azure.com/openai/v1/
LLM_MODEL_NAME=gpt-4

# =============================================================================
# NOTES:
# - For local embeddings, first run: pip install sentence-transformers torch
# - Common local models:
#   * BAAI/bge-large-en-v1.5 (Best quality, English)
#   * sentence-transformers/all-MiniLM-L6-v2 (Fast, good quality)
#   * intfloat/multilingual-e5-large (Multilingual support)
# - Azure OpenAI is recommended for production deployments
# - Replace "your-resource" with your Azure resource name
# =============================================================================
